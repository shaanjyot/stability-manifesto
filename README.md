# stability-manifesto

Next.js site with Supabase-backed PDF blogs.

## Supabase

Apply migrations in `supabase/migrations/` to your project (SQL Editor or `supabase db push` / linked CLI). New migrations add columns such as `visit_count` and RPCs used by the app.
